compute_accuracy <- function(predictions, actuals) {
  
  # preventing errors
  if (length(forecasts) != length(actuals))  {stop("Forecasts and actuals are not the same length")}
  
  # calculating accuracy
  acc <- 
  
  # returning accuracy
  return(acc)
}